const env = process.env.NODE_ENV;

const pre =  {
    registeredEmailMessage: 'This email address is already registered.',
    RegisteresrEmailSuccessfullyMessage: 'Newsletter : You have successfully subscribed to this newsletter.'
};

let config = pre;

export default config;